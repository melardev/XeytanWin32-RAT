#pragma once
/*
#include "../shared/net/packets/Packet.h"

class Packet;

class PacketProcessor
{
public:
	PacketProcessor();
	~PacketProcessor();
	static Packet* buildChatPacket(const PacketStruct& packet_struct);

	static Packet* buildPacket(const PacketStruct& packetStruct);
};
*/