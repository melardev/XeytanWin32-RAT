#pragma once
/*
#include "../models/net/Packet.h"
#include "../models/Client.h"

class Packet;

class PacketProcessor
{
public:
	PacketProcessor();
	~PacketProcessor();
	
	static Packet* buildChatPacket(const Packet& packet_struct);

	static Packet* buildPacket(Client& client, Packet& packet);
	// static Packet* buildPacket(UUID* uuid, const PacketStruct& packetStruct);
};

*/